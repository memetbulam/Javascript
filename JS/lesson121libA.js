export const PI = 3.14;

export const sum = (x, y) => { return x + y; }
export const square = (x) => { return x * x; }
export const hypotenuse = (x, y) => { return Math.sqrt(square(x) + square(y)); }
export let num = 7;
export const incrementNum = () => { return ++num; }